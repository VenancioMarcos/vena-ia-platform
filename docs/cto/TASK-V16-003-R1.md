# STATUS DE EXECUÇÃO — TASK-V16-003-R1

**Estado:** pronta para revisão do CTO; quatro workflows verdes no head `4c35100`.

## Falha, causa raiz e correção final

O primeiro gate real observou dois heartbeats, seis itens em `ready/scheduled` e
nenhum lease ou estado terminal. Diagnóstico allowlisted localizou
`InvalidRequestError` em `JobRepository.list_recoverable()` antes do claim.

O processo da API carregava `app.core.models_registry`, mas o entrypoint independente
`scripts.worker` não registrava o conjunto completo de modelos ORM. A configuração
tardia dos mappers falhava na primeira consulta PostgreSQL. A correção importa o
registro oficial no worker e o teste de regressão executa `configure_mappers()` em
subprocesso limpo. O diagnóstico seguro permanece deduplicado e sem IDs, tokens ou
conteúdo; evidence PASS só é enviada quando o gate conclui.

## Inconsistência registrada

A evidência inicial foi reclassificada como `HARNESS_ONLY_BASELINE`: duas APIs/dois
workers eram lógicos, auth/jobs usavam memory, MinIO não integrava o workflow e soak,
fila e cleanup continham valores produzidos pelo próprio simulador.

## Entrega R1

- duas APIs e dois workers como subprocessos oficiais independentes;
- PostgreSQL/pgvector, Redis e MinIO reais, fixados e descartáveis;
- auth, revogação, rate limit, jobs, claims, leases e idempotência compartilhados;
- jornada HTTP cross-instance com PDFs sintéticos, RAG local, histórico e isolamento;
- cancelamento, retry, interrupção/reinício de worker e lease recovery;
- backpressure real `503`/`Retry-After` e recuperação;
- pause/unpause MinIO com readiness fail-safe e retorno;
- soak integrado mínimo de 30 segundos e `NOT_MEASURED` sem zeros inventados;
- bundle externo, determinístico, checksummed, atômico e resistente a overwrite,
  concorrência, traversal e symlinks.

## Limites

CI descartável somente. R-034 parcialmente mitigado/monitorar; R-032 residual;
R-038 monitorado. Sem Release, deploy, piloto, SLA, SLO ou v1.7.

## Registro de entrega

### Objetivo

Substituir a evidência lógica do Package 3 por integração real controlada.

### Escopo

Gate de capacidade da PR #17; Packages 1 e 2 preservados.

### Arquivos criados

- `packages/ai/providers/deterministic.py`;
- `scripts/capacity_process_gate.py`;
- `docs/cto/TASK-V16-003-R1.md`.

### Arquivos modificados

Workflow, profile/policies, fila/AI, testes e documentação de capacidade/governança.

### Testes

Policy, Ruff, mypy e testes focais aprovaram `46 passed, 2 skipped`. No head
`4c35100606b394ce4a1533d7529b7a10deb9eac2`, os workflows oficiais passaram:
Backend `31131271003`, Frontend `31131271784`, Runtime Policy `31131271162` e
Controlled Capacity `31131271390`. O gate real comprovou a correção e publicou a
evidence segura; o run manual de confirmação `31131203892` também passou.

### Critérios de aceitação

O CI deve produzir `GUARDRAIL_RESULT=PASS`, artifact seguro e quatro workflows verdes.

### Próximos passos

Revisão do CTO na Draft PR #17. Merge, tag, Release e deploy permanecem proibidos.
