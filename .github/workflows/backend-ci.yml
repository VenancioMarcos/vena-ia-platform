name: Backend CI

on:
  pull_request:
    paths:
      - "apps/api/**"
      - "packages/ai/**"
      - "packages/__init__.py"
      - "pyproject.toml"
      - ".github/workflows/backend-ci.yml"
  push:
    branches:
      - main
    paths:
      - "apps/api/**"
      - "packages/ai/**"
      - "packages/__init__.py"
      - "pyproject.toml"
      - ".github/workflows/backend-ci.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install -e "apps/api[dev]"

      - name: Lint
        run: ruff check .

      - name: Type check AI Layer
        run: mypy packages/ai

      - name: Test
        working-directory: apps/api
        run: pytest
